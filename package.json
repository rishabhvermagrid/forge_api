{
  "name": "jobs-json-api",
  "version": "1.0.0",
  "description": "Jobs REST API powered by json-server",
  "scripts": {
    "start": "json-server --watch db.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3",
  }
}
